# OS
.DS_Store
Thumbs.db

# GDAL aux files
*.aux.xml
*.ovr

# Large source rasters (keep tiles, not originals)
*.tif
*.tiff

# Source zip archives (photos already extracted to folders)
*.zip

# Internal setup guide (keep local only)
SETUP.md
